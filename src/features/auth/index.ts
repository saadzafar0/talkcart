export { authService } from './services/authService';
export { validateRegister, validateLogin, validateEmail, validatePassword } from './utils/validators';
export type { User, LoginRequest, RegisterRequest, AuthResponse, UserPublic } from './types';
